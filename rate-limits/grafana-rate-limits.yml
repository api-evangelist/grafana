# Rate limits transcribed from the provider's own documentation — see provenance. Quoted, not derived.
generated: '2026-09-20'
method: searched
source: https://grafana.com/docs/loki/latest/operations/request-validation-rate-limits/
sources:
- https://grafana.com/docs/loki/latest/operations/request-validation-rate-limits/
provenance:
  tool: planning/api-economics/harvest_ratelimits.py
  run: 20260920T133210
  grounding: every limit_text was found verbatim in the fetched page text
published: false
note: Loki self-hosted page describing configurable ingestion rate limits and validation errors; rate
  limits are operator-configured per tenant, values given are defaults. Rate-limit errors return 429;
  validation errors return 400/413/260.
limit_count: 8
limits:
- name: per_stream_rate_limit default
  limit_text: 3MB for per_stream_rate_limit
  scope: unclear
  endpoint: per stream ingestion
- name: per_stream_rate_limit_burst default
  limit_text: 15MB for per_stream_rate_limit_burst
  scope: unclear
  endpoint: per stream ingestion
  kind: burst
- name: max_recv_msg_size default
  limit_text: max_recv_msg_size defaults to 100MB
  scope: unclear
  endpoint: distributor push request
- name: max_decompressed_size default
  limit_text: max_decompressed_size defaults to 5000MB
  scope: unclear
  endpoint: distributor push request
- name: stream label string limit
  limit_text: A single stream’s label string exceeds 16MB
  scope: unclear
  endpoint: push request
- name: max_label_name_length default
  limit_text: the default value of 1024
  scope: unclear
  endpoint: label names
- name: max_structured_metadata_size
  limit_text: 'max_structured_metadata_size (default: 64kb )'
  scope: unclear
  endpoint: structured metadata
- name: max_structured_metadata_entries_count
  limit_text: 'max_structured_metadata_entries_count (default: 128 )'
  scope: unclear
  endpoint: structured metadata
exceeded_status: 429
